# Copy this file to .env and fill in your values
# cp .env.example .env

# ── Database type ─────────────────────────────────────────────────────────────
# Supported values: mysql | postgresql
DB_TYPE=mysql

# ── MySQL connection ──────────────────────────────────────────────────────────
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=chatbot_db
DB_PORT=3306

# ── PostgreSQL connection (use when DB_TYPE=postgresql) ───────────────────────
# DB_HOST=localhost
# DB_USER=postgres
# DB_PASSWORD=your_pg_password
# DB_NAME=chatbot_db
# DB_PORT=5432
# DB_SCHEMA=public          # optional: restricts search_path to this schema

# ── LLM provider ─────────────────────────────────────────────────────────────
# Pick a provider and install its requirements file once:
#   pip install -r requirements-ollama.txt
#   pip install -r requirements-openai.txt
#   pip install -r requirements-anthropic.txt
#   pip install -r requirements-groq.txt

LLM_PROVIDER=ollama

# Model name — must match the chosen provider's model identifier:
# ollama:    llama3.1, mistral, gemma2, ...  (run: ollama pull <model>)
# openai:    gpt-4o, gpt-4o-mini, ...
# anthropic: claude-sonnet-4-6, claude-opus-4-7, claude-haiku-4-5-20251001, ...
# groq:      llama-3.1-70b-versatile, mixtral-8x7b-32768, ...
LLM_MODEL=llama3.1

# API key — required for openai / anthropic / groq; leave blank for ollama
LLM_API_KEY=

# Base URL — required for ollama; optional for openai-compatible custom endpoints
LLM_BASE_URL=http://localhost:11434

# ── PDF library ───────────────────────────────────────────────────────────────
PDF_DIR=./pdfs
CHROMA_DIR=./chroma_db

# ── Currency ─────────────────────────────────────────────────────────────────
# Symbol shown before monetary values in the chat UI
# Examples: $  €  £  ₹  ¥  ₩  ₺  ₦
CURRENCY_SYMBOL=$

# ── Flask ─────────────────────────────────────────────────────────────────────
SECRET_KEY=change-this-to-a-long-random-secret
